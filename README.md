# Elemdiagram
Python desktop tool for plotting element concentration data from Excel files. It reads .xls/.xlsx tables, uses the first column as sample IDs, plots all remaining columns as elements/parameters on a logarithmic Y-axis, handles missing or invalid values automatically, and allows loading new files or saving figures through a simple menu interface.
